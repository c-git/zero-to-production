application:
  port: 8000
  host: 0.0.0.0
  hmac_secret: "super-long-and-secret-random-key-needed-to-verify-message-integrity"
database:
  host: "127.0.0.1"
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "newsletter"
  require_ssl: false
email_client:
  sender_email: "test@gmail.com" # If using SMTP ensure this is set to the Gmail address that matches the password in kind.smtp.password
  timeout_milliseconds: 10000
  kind:
#    url:
#      base_url: "localhost"
#      authorization_token: "my-secret-token"
    smtp:
      password: "use an app password here https://support.google.com/mail/answer/185833" # Note Google doesn't really like these, but it  works for testing
      smtp_server: "smtp.gmail.com"
redis_uri: "redis://127.0.0.1:6379"